package org.example.base

object LoginManager {
    fun verifyAddress(addr: String) : Boolean {
        // TODO
        return true
    }

    fun onLogin(id: Long, token: String) : Long {
        return id;
    }
}